import { PlayFunction } from "./common.js";

export const play: PlayFunction = () =>
	// playersColumns,
	// startOfGame,
	// discard,
	// drawFromStock,
	// drawFromDiscard,
	// gameIndex,
	// roundIndex,
	// turnIndex,
	// lastTurn,
	// playersGameScores,
	{
		// ...

		return {
			action: "flip_two_cards",
			locations: [
				{ x: 1, y: 1 },
				{ x: 1, y: 2 },
			],
		};
	};
